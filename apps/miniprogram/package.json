{
  "name": "@dishflow/miniprogram",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit && echo 'miniprogram TS compile OK; WeChat devtools build for production'"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^4.0.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
